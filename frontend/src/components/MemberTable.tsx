import type { MemberRow } from '../types';
export function MemberTable({ members }: { members: MemberRow[] }): JSX.Element { if (!members.length) return <p>No members match these filters.</p>; return <div className="table-wrap"><table><thead><tr><th scope="col">Name</th><th scope="col">DOB</th><th scope="col">Plan</th><th scope="col">PCP</th><th scope="col">Risk</th><th scope="col">Open gaps</th></tr></thead><tbody>{members.map(member => <tr key={member.member_id}><td>{member.name}</td><td>{member.dob}</td><td>{member.plan}</td><td>{member.pcp}</td><td>{member.risk_level}</td><td>{member.open_gaps}</td></tr>)}</tbody></table></div>; }
