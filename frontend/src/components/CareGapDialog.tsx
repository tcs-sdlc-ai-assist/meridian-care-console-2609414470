import { useState,type FormEvent } from 'react'; export function CareGapDialog({gapId,onClose}:{gapId:string;onClose:()=>void}):JSX.Element{const [reason,setReason]=useState('');async function submit(e:FormEvent){e.preventDefault();await fetch(`/api/v1/members/MEM-1001/gaps/${gapId}/close`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('meridian-token')}`},body:JSON.stringify({reason})});onClose();}return <div role="dialog" aria-modal="true" aria-label="Close care gap"><form className="card" onSubmit={submit}><h2>Close care gap</h2><label htmlFor="reason">Closure reason</label><textarea id="reason" value={reason} onChange={e=>setReason(e.target.value)} required/><button className="button button-primary">Save closure</button><button type="button" className="button button-secondary" onClick={onClose}>Cancel</button></form></div>;}
