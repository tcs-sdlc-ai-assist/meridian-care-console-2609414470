import '@testing-library/jest-dom/vitest';import {expect,test,vi} from 'vitest';import {render,screen} from '@testing-library/react';import MemberDetailPage from './MemberDetailPage';vi.stubGlobal('fetch',vi.fn().mockResolvedValue({json:()=>Promise.resolve({name:'Rosa Diaz',dob:'1958-04-12',plan:'Meridian Gold',pcp:'Dr. Patel',risk_level:'high',ssn:'***-**-6789',mbi:'****MK73',gaps:[]})}));test('renders masked member identifiers',async()=>{render(<MemberDetailPage/>);expect(await screen.findByText(/SSN \*\*\*-\*\*-6789/)).toBeVisible();});
