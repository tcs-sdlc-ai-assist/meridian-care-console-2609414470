import '@testing-library/jest-dom/vitest'; import { expect,test,vi } from 'vitest'; import { render,screen } from '@testing-library/react'; import DashboardPage from './DashboardPage'; vi.mock('../api/client',()=>({dashboard:vi.fn().mockResolvedValue({metrics:[{label:'Open care gaps',value:2}],attention:['Rosa Diaz'],activity:['Seeded'],chart:[2,1,1]})})); test('renders dashboard attention data',async()=>{render(<DashboardPage/>);expect(await screen.findByText('Rosa Diaz')).toBeVisible();});
